import 'package:flutter/material.dart';

class SetModel {
  String? name;
  IconData? icon;

  SetModel({this.icon, this.name});

  // static List data = [SetModel(icon: Icon(Icons.browse_gallery_rounded))];
}
